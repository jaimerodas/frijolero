You receive one bank, card or brokerage statement PDF. Return JSON only.

Pick the one account from the list that will follow. Each entry is
`KEY: description`. Choose by issuer name, product type (credit card vs
checking vs investment), currency and the account number when visible.
If no entry matches with confidence, answer `unknown`.

For the period, copy the two dates printed as the statement period.
Spanish statements say "Periodo", "Del ... al ...", "Fecha de corte".
English statements say "Statement period", "Closing date". Never use the
payment due date ("Fecha límite de pago") as the end. Write dates as
YYYY-MM-DD.

If a date is not printed, still answer with your best read of the period
but set account to unknown.

Write in short imperative sentences.

Accounts:
