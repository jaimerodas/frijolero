{
  "name": "transaction_list",
  "strict": true,
  "schema": {
    "type": "object",
    "properties": {
      "transactions": {
        "type": "array",
        "description": "A list of transactions, each with a date, amount, and full description.",
        "items": {
          "type": "object",
          "properties": {
            "date": {
              "type": "string",
              "description": "Date of the transaction in YYYY-MM-DD format.",
              "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
            },
            "amount": {
              "type": "number",
              "description": "The transaction amount. Should include decimals."
            },
            "description": {
              "type": "string",
              "description": "Full description of the transaction, including all available detail and supporting multiple lines as needed."
            }
          },
          "required": ["date", "amount", "description"],
          "additionalProperties": false
        }
      }
    },
    "required": ["transactions"],
    "additionalProperties": false
  }
}
