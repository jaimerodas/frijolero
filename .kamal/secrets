# Kamal reads this file at deploy time. Each value comes from the shell that
# runs `kamal deploy`, so no secret lives in the repo. Export them first, or
# replace the references with `$(op read ...)` style commands.
KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD
OPENAI_API_KEY=$OPENAI_API_KEY
APP_PASSWORD=$APP_PASSWORD
B2_KEY_ID=$B2_KEY_ID
B2_KEY=$B2_KEY
GIT_TOKEN=$GIT_TOKEN
